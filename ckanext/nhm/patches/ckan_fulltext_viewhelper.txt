diff --git a/ckan/public/base/javascript/view-filters.js b/ckan/public/base/javascript/view-filters.js
index c412969..206fb8d 100644
--- a/ckan/public/base/javascript/view-filters.js
+++ b/ckan/public/base/javascript/view-filters.js
@@ -98,8 +98,11 @@ this.ckan.views.filters = (function (queryString) {
 
   var api = {
     get: get,
+    getFullText: getFullText,
     set: set,
+    setFullText: setFullText,
     setAndRedirectTo: setAndRedirectTo,
+    setFullTextAndRedirectTo: setFullTextAndRedirectTo,
     unset: unset,
     _searchParams: {},
     _initialize: _initialize,
@@ -116,12 +119,22 @@ this.ckan.views.filters = (function (queryString) {
     }
   }
 
+  function getFullText(){
+    return api._searchParams.q || null;
+  }
+
   function set(name, value) {
     var url = window.location.href;
 
     setAndRedirectTo(name, value, url);
   }
 
+  function setFullText(q){
+    var url = window.location.href;
+
+    setFullTextAndRedirectTo(q, url);
+  }
+
   function setAndRedirectTo(name, value, url) {
     api._searchParams.filters = api._searchParams.filters || {};
     api._searchParams.filters[name] = value;
@@ -131,6 +144,14 @@ this.ckan.views.filters = (function (queryString) {
     return api;
   }
 
+  function setFullTextAndRedirectTo(q, url){
+    api._searchParams.q = q;
+
+    _redirectTo(url);
+
+    return api;
+  }
+
   function unset(name, value) {
     var thisFilters = get(name);
 
diff --git a/ckanext/reclineview/theme/public/recline_view.js b/ckanext/reclineview/theme/public/recline_view.js
index 89936d5..1c28f35 100644
--- a/ckanext/reclineview/theme/public/recline_view.js
+++ b/ckanext/reclineview/theme/public/recline_view.js
@@ -65,6 +65,9 @@ this.ckan.module('reclinepreview', function (jQuery, _) {
         $.each(filters, function (field,values) {
           query.addFilter({type: 'term', field: field, term: values});
         });
+        if (window.parent.ckan.views.filters.getFullText()){
+          query.set({ q: window.parent.ckan.views.filters.getFullText()});
+        }
       }
 
       dataset.query(query);
